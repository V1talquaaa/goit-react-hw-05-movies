// import { NavLink } from "react-router-dom"

// const FilmsList = ({films}) => {

//     return films.map(({title, id}) => {
//         return <li key={id} props={id}>{title}</li>
//     })

// }

// export default FilmsList